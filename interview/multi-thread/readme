  1，进程是处于运行过程中的程序，并且具有一定的独立的功能，进程是系统进行资源分配和调度的一个独立单位。
  
  2，进程的三个特征：
      独立性，每个进程都有自己私有的地址空间。
      动态性，程序只是一个静态的指令集和，而进程都拥有自己私有的指令集和。
      并发性，多个进程在单个处理器上并发执行， 多个进程之间不会相互影响
      
  3，多线程的优点
      进程之间不能共享内存， 而线程可以
      创建线程不需要重新分配系统资源，多线程的多任务并发比多进程的效率要高
      java本身内置了多线程的支持功能
